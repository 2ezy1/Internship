# PostgreSQL (default). If you get "password authentication failed", use SQLite below or fix Postgres.
# DATABASE_URL=postgresql+psycopg2://postgres:bisumain26@172.20.10.4:5432/devices_db
# SQLite (no Postgres needed): uncomment next line and comment out the line above
DATABASE_URL=sqlite:///./devices.db
JWT_SECRET_KEY=bisumain26
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30